package com.gft.wrk2025carrito.shopping_cart.infrastructure.repository.impl;

import lombok.RequiredArgsConstructor;
import org.springframework.stereotype.Repository;

@Repository
@RequiredArgsConstructor
public class CarEntityRepositoryImpl {

//    private final CartEntityRepository cartEntityRepository;
//
//    @Override
//    public Cart findById(CartId cartId) {
//        return cartEntityRepository.findById(cartId.id()).map(cartEntity -> ).orElse(null);
//    }
}
